# Origem do backend em PRODUÇÃO (ex.: https://api.estoque-ceo.ufpe.br).
# Em desenvolvimento, deixe vazio: o Vite faz proxy de /api -> localhost:3000
# (ver vite.config.ts), então o front usa o padrão "/api".
VITE_API_BASE_URL=
